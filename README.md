# 🤖 QR Code Telegram Bot

Бот для быстрого преобразования текста в QR коды с возможностью просмотра и печати.

## ✨ Возможности

- ✅ Преобразует любой текст в QR код
- ✅ Отправляет QR код фото в Telegram
- ✅ Ссылка на веб-интерфейс для просмотра в браузере
- ✅ Возможность печати QR кода
- ✅ Размещение исходного текста под кодом
- ✅ Фильтрация (обрабатывает только текст, игнорирует гифы, стикеры и т.д.)
- ✅ Готов к развертыванию на Railway

## 🛠️ Требования

- Python 3.9+
- Telegram Bot Token (от @BotFather)
- Railway Account (для хостинга)

## 📦 Зависимости

```bash
pip install -r requirements.txt
```

## 🚀 Развертывание на Railway

### 1. Создание бота в Telegram

1. Напишите @BotFather в Telegram
2. Нажмите `/start` и следуйте инструкциям
3. Создайте нового бота (`/newbot`)
4. Скопируйте токен бота

### 2. Развертывание на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите ваш GitHub репозиторий
4. В настройках проекта добавьте переменные окружения:
   - `TELEGRAM_TOKEN`: Ваш токен бота
   - `WEBHOOK_URL`: `https://your-app-name.railway.app/`
   - `PORT`: `8443`

5. Нажмите Deploy

### 3. Получение URL приложения

После развертывания Railway предоставит URL вашего приложения. Это будет ваш `WEBHOOK_URL`.

## 🧪 Локальное тестирование

### 1. Создайте файл `.env`

```bash
cp .env.example .env
```

Отредактируйте `.env` с вашими данными.

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Запустите бота

```bash
python main.py
```

**Примечание**: Для локального тестирования нужен publically доступный URL (используйте ngrok или похожий инструмент).

## 📝 Использование

1. Напишите боту `/start`
2. Отправьте любой текст
3. Получите:
   - 📱 Фото QR кода
   - 🌐 Ссылку на просмотр в браузере
   - 📝 Исходный текст

## 🔗 API Endpoints

- `GET /qr/<qr_id>` - Просмотр QR кода в браузере
- `GET /qr/<qr_id>/image` - Получить изображение QR кода (PNG)

## 🛡️ Фильтрация

Бот автоматически:
- ✅ Обрабатывает только текстовые сообщения
- ❌ Игнорирует гифы, стикеры, фото, видео и другие медиа
- ❌ Игнорирует команды (кроме /start)

## 📄 Структура проекта

```
.
├── main.py              # Основной код бота
├── requirements.txt     # Зависимости
├── Procfile            # Конфигурация для Railway
├── .env.example        # Пример файла окружения
├── templates/
│   └── qr.html         # HTML страница просмотра QR кода
└── README.md           # Этот файл
```

## 🐛 Решение проблем

### Ошибка: "TELEGRAM_TOKEN не установлен"
- Проверьте, что вы установили переменную окружения `TELEGRAM_TOKEN`
- На Railway это делается в Project Settings → Variables

### Бот не отвечает
- Убедитесь, что `WEBHOOK_URL` правильно установлена
- Проверьте логи на Railway

### QR код не открывается в браузере
- Чекните, что `WEBHOOK_URL` совпадает с URL приложения на Railway
- URL должен заканчиваться на `/`

## 📞 Поддержка

Если у вас возникли проблемы, проверьте:
1. Все ли переменные окружения установлены
2. Правильный ли токен бота
3. Интернет соединение
4. Логи приложения на Railway

## 📄 Лицензия

MIT License

---

**Создано с ❤️ для удобства**
